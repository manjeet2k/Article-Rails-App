<nav class="navbar navbar-expand-md navbar-light">
  <div class="container pb-0">
    <a class="navbar-brand" href="/" title="Home">Sample App</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>    
    <% if logged_in? %>
      <ul class="navbar-nav ml-auto">
        <%= link_to fa_icon("plus", text: "New Article", class: "mr-1", style: "font-size: 18px;"), new_article_path, class: "nav-link", title: "Create new article" unless current_user.admin? %>
        <%= link_to fa_icon("download", text: "Download PDF", class: "mr-1", style: "font-size: 18px;"), download_path(format: 'pdf'), class: "nav-link", title: "download pdf" unless current_user.admin?  %>
        <%= link_to fa_icon("plus", text: "New Category", class: "mr-1", style: "font-size: 18px;"), new_category_path, class: "nav-link", title: "Create new category" if current_user.admin? %>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            <i class="fa fa-user-o mr-1", style="font-size: 18px;", aria-hidden="true"></i> <%= current_user.name.split(" ")[0] %>
          </a>
          <div class="dropdown-menu" aria-labelledby="navbarDropdown">
            <%= link_to "Categories", categories_path, class: "dropdown-item" if current_user.admin? %>
            <div class="dropdown-divider"></div>
            <a class="dropdown-item" href="/logout" title="logout" data-method="delete"><i class="fa fa-sign-out" aria-hidden="true"></i>
              logout
            </a>  
          </div>
        </li>
      </ul>            
    <% else %>
      <ul class="navbar-nav ml-auto">
        <li><%= link_to 'Login',   login_path,  class: 'nav-link' %></li>
        <li><%= link_to 'Sign up', signup_path, class: 'nav-link' %></li>
      </ul>
    <%end%>
  </div>
</nav>